#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/Users/aleks/dev/rescript/http/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="$NODE_PATH:/Users/aleks/dev/rescript/http/node_modules/.pnpm/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../../rescript@10.1.2/node_modules/rescript/lib/bstracing" "$@"
else
  exec node  "$basedir/../../../../../../rescript@10.1.2/node_modules/rescript/lib/bstracing" "$@"
fi
